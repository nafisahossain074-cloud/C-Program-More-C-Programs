#include <stdio.h>

int main() {
    FILE *fp = fopen(__FILE__, "r");
    char ch;

    if(fp == NULL) {
        printf("Cannot open file.\n");
        return 1;
    }

    while((ch = fgetc(fp)) != EOF) {
        printf("%c", ch);
    }

    fclose(fp);
    return 0;
}
